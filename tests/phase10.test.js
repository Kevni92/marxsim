'use strict';const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');['../src/config.js','../src/rng.js','../src/state.js','../src/map/mapModel.js','../src/sim/clock.js','../src/sim/population.js','../src/sim/labor.js','../src/sim/production.js','../src/sim/market.js','../src/sim/competition.js','../src/sim/investment.js','../src/sim/infrastructure.js','../src/sim/housing.js','../src/sim/politics.js','../src/sim/metrics.js','../src/sim/engine.js','../src/analysis/monteCarlo.js'].forEach(p=>require(p));const MS=globalThis.MarxSim,scenario=JSON.parse(fs.readFileSync(path.join(__dirname,'../data/scenario.json'),'utf8')),rows=MS.runMonteCarlo(scenario,{seeds:100,ticks:72,startSeed:2000}),d=MS.diagnoseMonteCarlo(rows);assert.equal(rows.length,100);assert.ok(d.finite,'all runs must remain finite');assert.equal(d.identicalEndStates,false,'different seeds must not collapse into identical end states');assert.ok(d.explosiveRate<.25,'extreme explosions must remain exceptional');const p=MS.comparePolicyPresets(scenario,2026,96);assert.deepEqual(Object.keys(p).sort(),['investmentState','laborRegulation','laissezFaire','socialSpending'].sort());const signatures=Object.values(p).map(x=>`${x.unemploymentRate.toFixed(6)}:${x.capital.toFixed(2)}:${x.treasury.toFixed(1)}`);assert.ok(new Set(signatures).size>1,'policy paths must produce different trade-offs');console.log(`Phase 10 OK: runs=${d.runs}, collapse=${(d.collapseRate*100).toFixed(1)}%, spread=${d.capitalSpread.toFixed(1)}`);
