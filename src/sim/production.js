(function(root){'use strict';const MS=root.MarxSim=root.MarxSim||{},clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function inputFactor(state,firm){const sector=state.sectors.find(s=>s.id===firm.sector),req=sector&&sector.input_requirements||{},availability=state.economy.inputAvailability||{};let shortage=0,total=0;for(const [id,shareRaw] of Object.entries(req)){const share=Math.max(0,Number(shareRaw)||0),a=availability[id]==null?1:availability[id];total+=share;shortage+=share*Math.max(0,1-a);}if(total<=0)return 1;return clamp(1-shortage*.85,.55,1.03);}
MS.runProduction=function runProduction(state){for(const firm of state.firms){if(!firm.active){firm.output=0;continue;}const capitalFactor=Math.pow(Math.max(0.05,firm.capital_stock/firm.reference_capital),1-MS.CONFIG.alpha),laborFactor=Math.pow(Math.max(0,firm.employees),MS.CONFIG.alpha),hours=(state.policies&&state.policies.workingHoursLimit)||12,hoursFactor=Math.max(0.68,Math.min(1,hours/12)),strikeFactor=Math.max(0.12,1-(firm.strike_intensity||0)*0.85),materials=inputFactor(state,firm),potential=firm.base_productivity*0.10*firm.technology*laborFactor*capitalFactor*hoursFactor*strikeFactor*materials;firm.input_availability=materials;firm.output=Math.max(0,Math.min(firm.capacity,potential));firm.inventory=Math.max(0,firm.inventory+firm.output);firm.age+=1;}};
MS.productionInputFactor=inputFactor;
})(typeof globalThis!=='undefined'?globalThis:window);
