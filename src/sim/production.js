(function(root){'use strict';const MS=root.MarxSim=root.MarxSim||{};MS.runProduction=function runProduction(state){for(const firm of state.firms){if(!firm.active){firm.output=0;continue;}const capitalFactor=Math.pow(Math.max(0.05,firm.capital_stock/firm.reference_capital),1-MS.CONFIG.alpha),laborFactor=Math.pow(Math.max(0,firm.employees),MS.CONFIG.alpha),hours=(state.policies&&state.policies.workingHoursLimit)||12,hoursFactor=Math.max(0.68,Math.min(1,hours/12)),strikeFactor=Math.max(0.12,1-(firm.strike_intensity||0)*0.85),potential=firm.base_productivity*0.10*firm.technology*laborFactor*capitalFactor*hoursFactor*strikeFactor;firm.output=Math.max(0,Math.min(firm.capacity,potential));firm.inventory=Math.max(0,firm.inventory+firm.output);firm.age+=1;}};})(typeof globalThis!=='undefined'?globalThis:window);
