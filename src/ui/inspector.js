(function(root){'use strict';const MS=root.MarxSim=root.MarxSim||{};
MS.getFirmCard=function(state,firmId){const f=state.firms.find(x=>x.id===firmId);if(!f)return null;const sector=state.sectors.find(s=>s.id===f.sector),district=state.districts.find(d=>d.id===f.district),wageBill=(f.employees||0)*(f.wage_offer||0),margin=(f.profit||0)/Math.max(1,f.revenue||0);return {id:f.id,name:f.name,sector:sector&&sector.name||f.sector,district:district&&district.name||f.district,employees:f.employees||0,wage:f.wage_offer||0,wageBill,output:f.output||0,sales:f.sales||0,inventory:f.inventory||0,revenue:f.revenue||0,profit:f.profit||0,margin,capital:f.capital_stock||0,technology:f.technology||0,marketShare:f.market_share||0,strike:f.strike_intensity||0,active:f.active};};
MS.renderFirmInspector=function(el,state,firmId){if(!el)return;const c=MS.getFirmCard(state,firmId);if(!c){el.innerHTML='<p class="quiet">Betrieb auf der Karte auswählen.</p>';return;}el.innerHTML=`<article class="firm-card"><header><small>${c.sector} · ${c.district}</small><h4>${c.name}</h4></header><dl><dt>Beschäftigte</dt><dd>${Math.round(c.employees)}</dd><dt>Lohnangebot</dt><dd>${c.wage.toFixed(2)}</dd><dt>Produktion / Absatz</dt><dd>${c.output.toFixed(1)} / ${c.sales.toFixed(1)}</dd><dt>Lager</dt><dd>${c.inventory.toFixed(1)}</dd><dt>Gewinn</dt><dd>${c.profit.toFixed(1)}</dd><dt>Marge</dt><dd>${(c.margin*100).toFixed(1)} %</dd><dt>Kapitalstock</dt><dd>${c.capital.toFixed(1)}</dd><dt>Marktanteil</dt><dd>${(c.marketShare*100).toFixed(1)} %</dd></dl>${c.strike>0?'<div class="red-stamp">ARBEITSKAMPF</div>':''}</article>`;};
})(typeof globalThis!=='undefined'?globalThis:window);
